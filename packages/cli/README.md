# CLI shell — specification only

Retained in step 10 as a developer/power-user shell over the same engine. The Windows .exe, installer and double-click wizard are cancelled. The primary friends/family interface is the offline single HTML file in packages/report; CLI is not a prerequisite for it and does not revive packaging or app-store plans.

Reuse validated reader, source selection/disclosure, lifecycle/burst and report-model contracts. Node persistence may retain explicit --output filesystem destinations and immutable snapshots; the shared report model must not depend on those paths. No network, AI, file deletion or quarantine policy in core/CLI. Only declared payload paths can be opened, and actual read outcomes accompany disclosure. Current scripts remain developer tools; no packaged CLI exists yet.

Preserve optional local annotations through explicit bundle import/export without modifying source snapshots. User meaning is never generated. Use last recorded here, no normative metrics or failure framing. See ../../docs/browser-and-annotations.md and ../../docs/BUILD-CONTRACT.md. MCP is built after this shell as the final step-10 milestone.
